*.iml
.gradle
/local.properties

# The whole IDE directory, not the handful of files the template listed. What was
# tracked was per-machine state - misc.xml pins the JDK by its local name, gradle.xml
# lists locally linked modules - so it churned on every clone that opened the project.
/.idea/

.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
